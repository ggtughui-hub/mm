-- =====================================================
-- SERVER-SIDE LICENSE / ACCESS PROTECTION
-- الفكرة: لا ترسل السورس الحساس للعميل
-- =====================================================

local HttpService = game:GetService("HttpService")

local API_URL = "https://YOUR-SERVER.example/verify"
local LICENSE_KEY = "YOUR_KEY"

local function verify()
    local ok, result = pcall(function()
        return HttpService:RequestAsync({
            Url = API_URL,
            Method = "POST",
            Headers = {
                ["Content-Type"] = "application/json"
            },
            Body = HttpService:JSONEncode({
                key = LICENSE_KEY,
                jobId = game.JobId,
                placeId = game.PlaceId
            })
        })
    end)

    if not ok or not result.Success then
        return false
    end

    local data = HttpService:JSONDecode(result.Body)
    return data.allowed == true
end

if not verify() then
    warn("Access denied")
    return
end

-- هنا شغّل الوظائف المسموح بها فقط.
-- لا تضع السورس السري داخل العميل.
